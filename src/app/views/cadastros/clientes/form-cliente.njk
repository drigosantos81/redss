{% extends 'layout.njk' %}

{% block head %}
  <title>Novo Centro de Custo Cliente</title>
{% endblock %}

{% block content %}
  <div class="container-fluid">

<!-- Page Heading -->
    <div class="novo-botao-func">
      <h1 class="h3 mb-2 text-gray-800">Formulário de Cadastro de Novo Cliente ou Obra</h1>
    </div>

    <p class="mb-4">Preencha todos os campos para incluir o novo funcionário.</p>

    <div class="mb-4">
      <div class="cad-cliente">
        <form action="/cadastros/clientes" method="POST">
          <div class="form-cliente">
            <div> {# NOME #}
              <div class="rotulo">
                NOME: 
              </div>
              <input
                class="campos-padrao-edite"
                placeholder="Nome / Razão Social"
                type="text"
                name="nome"
              >
            </div>

            <div> {# CEI #}
              <div class="rotulo">
                CEI: 
              </div>
              <input
                class="campos-padrao-edite"
                placeholder="00.000.00000/00"
                type="text"
                name="cei"
                onkeydown="Mask.apply(this, 'cei')"
              >
            </div>

             <div> {# CNPJ #}
              <div class="rotulo">
                CNPJ ou CPF: 
              </div>
              <input
                class="campos-padrao-edite"
                placeholder="Nº do CNPJ ou CPF"
                type="text"
                name="cnpj_cpf"
                onkeydown="Mask.apply(this, 'cpfCnpj')"
              >
            </div>

            <div> {# VALOR CONTRATO #}
              <div class="rotulo">
                Valor do Contrato: 
              </div>
              <input
                class="campos-padrao-edite"
                placeholder="R$ 000.000,00"
                type="text"
                name="valor_contrato"
                onkeydown="Mask.apply(this, 'formatBRL')"
              >
            </div>

            <div> {# DATA CONTRATO #}
              <div class="rotulo">
                Data da assinatura do Contrato: 
              </div>
              <input 
                class="data"
                placeholder="DD/MM/AAAA"
                type="text"
                name="data_contrato"
                onkeydown="Mask.apply(this, 'data')"
              >
            </div>

            <div> {# CEP #}
              <div class="rotulo">CEP: </div>
              <input
                class="cep"
                placeholder="00.000-000"
                type="text"
                name="cep"
                onkeydown="Mask.apply(this, 'cep')"
                >
            </div>

            <div> {# ENDEREÇO #}
              <div class="rotulo">Logradouro: </div>
              <input
                class="campos-padrao-edite"
                placeholder="Rua, Av."
                type="text"
                name="endereco"
                >
            </div>

            <div> {# NÚMERO #}
              <div class="rotulo">Nº: </div>
              <input
                class="campo-6-digitos"
                placeholder="Nº ou S/N."
                type="text"
                name="numero_end"
                >
            </div>

            <div> {# BAIRRO #}
              <div class="rotulo">Bairro: </div>
              <input
                class="campos-padrao-edite"
                placeholder="Nome do bairro."
                type="text"
                name="bairro"
                >
            </div>

            <div> {# PAÍS #}
              <div class="rotulo">País: </div>
              <select id="pais" name="pais"></select>
            </div>

            <div> {# UF #}
              <div class="rotulo">UF: </div>
              <select id="estado" name="uf"></select>
            </div>

            <div> {# CIDADE #}
              <div class="rotulo">Cidade: </div>
                <select id="cidade" name="cidade"></select>
            </div>

            <div> {# DATA FIM CONTRATO #}
              <div class="rotulo">Data do Encerramento do Contrato</div>
              <input
                class="data"
                placeholder="DD/MM/AAAA"
                type="text"
                name="data_fim_contrato"
                onkeydown="Mask.apply(this, 'data')"
              >
            </div>
          </div>

          <button type="submit">Cadastrar</button>
          <a id="cancelar" href="/cadastros/clientes">Cancelar</a>
        </form>
      </div>
    </div>

  </div>
  <!-- /.container-fluid -->
{% endblock %}

{% block scripts %}
  <script src="/js/scriptsApiExterna.js"></script>
{% endblock scripts %}
